version: "3.9"

services:
  api:
    build: .
    ports:
      - "8847:8847"
    volumes:
      - uploads:/app/uploads
      - results:/app/results
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  uploads:
  results:
